apiVersion: v1
kind: Service
metadata:
  name: continux-metrics
  namespace: pipeline
  labels:
    app: continux-metrics
spec:
  selector:
    app: continux-metrics
  ports:
    - name: metrics
      port: 9108
      targetPort: metrics
