# Chuẩn hóa ký tự xuống dòng: mọi file văn bản trong repo dùng LF.
* text=auto eol=lf

# Script shell luôn dùng LF, không dùng CRLF.
*.sh text eol=lf

# File chỉ chạy trên Windows giữ CRLF.
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf
