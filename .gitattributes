# Normalize line endings: all text files use LF in repo
* text=auto eol=lf

# Shell scripts must always be LF — never CRLF
*.sh text eol=lf

# Windows-only files keep CRLF
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf
