\section{SQL cốt lõi}\label{app:core-sql}
\subsection{Nguồn sự kiện, bảng tra cứu và MV nền}
\begin{lstlisting}[language=SQL]
CREATE SOURCE IF NOT EXISTS nyc_taxi_src (
    event_id VARCHAR,
    event_time TIMESTAMPTZ,
    pickup_time TIMESTAMPTZ,
    pu_location_id INT,
    do_location_id INT,
    fare_amount DOUBLE PRECISION,
    trip_distance DOUBLE PRECISION
) WITH (
    connector = 'kafka',
    topic = 'nyc-taxi-events',
    properties.bootstrap.server = 'redpanda.redpanda:9093',
    scan.startup.mode = 'earliest'
) FORMAT PLAIN ENCODE JSON;

CREATE TABLE IF NOT EXISTS tlc_zone (
    location_id INT PRIMARY KEY,
    borough VARCHAR,
    zone VARCHAR,
    service_zone VARCHAR
) WITH (
    connector = 's3',
    s3.bucket_name = 'tlc-zone',
    s3.endpoint_url = 'http://minio.minio.svc.cluster.local:9000',
    enable_config_load = 'true',
    match_pattern = 'taxi_zone_lookup.csv'
) FORMAT PLAIN ENCODE CSV (delimiter = ',', without_header = false);

CREATE MATERIALIZED VIEW IF NOT EXISTS mv_zone_stats_blue AS
SELECT z.borough, z.zone, COUNT(*) AS trip_count,
       SUM(t.fare_amount) AS total_fare,
       AVG(t.trip_distance) AS avg_distance
FROM nyc_taxi_src t
JOIN tlc_zone z ON t.pu_location_id = z.location_id
GROUP BY z.borough, z.zone;

CREATE MATERIALIZED VIEW IF NOT EXISTS mv_zone_stats AS
SELECT * FROM mv_zone_stats_blue;
\end{lstlisting}

\subsection{MV Green và thao tác hoán đổi}
\begin{lstlisting}[language=SQL]
CREATE MATERIALIZED VIEW IF NOT EXISTS mv_zone_stats_green AS
SELECT z.borough, z.zone, COUNT(*) AS trip_count,
       SUM(t.fare_amount) AS total_fare,
       AVG(t.trip_distance) AS avg_distance
FROM nyc_taxi_src t
JOIN tlc_zone z ON t.pu_location_id = z.location_id
WHERE t.fare_amount >= 0
  AND t.trip_distance >= 0
GROUP BY z.borough, z.zone;

ALTER MATERIALIZED VIEW mv_zone_stats
SWAP WITH mv_zone_stats_green;
\end{lstlisting}

\subsection{Đích ghi Iceberg}
\begin{lstlisting}[language=SQL]
CREATE SINK IF NOT EXISTS sink_zone_stats FROM mv_zone_stats
WITH (
    connector = 'iceberg',
    type = 'upsert',
    primary_key = 'borough,zone',
    catalog.type = 'storage',
    warehouse.path = 's3://iceberg-data/',
    s3.endpoint = 'http://minio.minio.svc.cluster.local:9000',
    enable_config_load = 'true',
    database.name = 'nyc',
    create_table_if_not_exists = 'true',
    table.name = 'zone_stats'
);
\end{lstlisting}

\section{Cấu hình vận hành trích yếu}\label{app:config}
\begin{lstlisting}
# Redpanda topic
topics:
  - name: nyc-taxi-events
    partitions: 3
    replicationFactor: 1
    config:
      retention.ms: "86400000"

# Vector profiles: configured source-side limits
smoke:            2 events/s
benchmark-low:    1000 events/s
benchmark-medium: 5000 events/s
benchmark-high:   10000 events/s

# RisingWave state store
stateStore:
  s3:
    enabled: true
    endpoint: http://minio.minio.svc.cluster.local:9000
    bucket: rw-checkpoint
\end{lstlisting}

\section{Chỉ mục bằng chứng}\label{app:evidence}
Bốn gói bằng chứng chính thức nằm ngoài Git tại:

\begin{lstlisting}
evidence/finalize/20260531-four-profiles/
  SHA256SUMS.txt
  summary.csv
  sensitive-scan.txt
  runs/
    20260531-172050/  # smoke
    20260531-173434/  # benchmark-low
    20260531-174621/  # benchmark-medium
    20260531-175701/  # benchmark-high
\end{lstlisting}

Mỗi gói có \texttt{41} tệp. Các tệp cần ưu tiên khi kiểm tra lại số liệu là:

\begin{itemize}
\item \texttt{00-run-id.txt}: cấu hình và giới hạn phát cấu hình.
\item \texttt{04-mv-progress.txt}, \texttt{04-mv-final.txt}: tiến trình và số liệu chốt sau phát lại.
\item \texttt{04-iceberg-final.txt}: đối tượng Iceberg.
\item \texttt{05-green-ready-samples.txt}: MV Green đã sẵn sàng trước chuyển giao.
\item \texttt{05-query-loop-during-cutover.txt}, \texttt{05-duration-and-errors.txt}: truy vấn trong lúc hoán đổi, thời lượng và lỗi.
\item \texttt{05-final-*}: SQL, Vector, sức khỏe cụm và Argo CD ở bước xác minh cuối.
\item \texttt{06-*}: kết quả dọn trạng thái sinh ra.
\end{itemize}

\section{Danh sách hình minh chứng}\label{app:figures}
PDF tích hợp năm hình minh chứng được dựng hoặc chụp từ hệ thống Continux 2.1.0 và bốn lượt đo chính thức:

\begin{enumerate}
\item Sơ đồ kiến trúc, luồng xử lý và quy trình chuyển giao Continux 2.1.0.
\item Bảng điều khiển \texttt{streaming-perf} của lượt \texttt{benchmark-high}, thể hiện các đợt lưu lượng, offset và xử lý luồng.
\item Bảng điều khiển \texttt{resource-util} trong cửa sổ phát lại \texttt{benchmark-high}, thể hiện mức sẵn sàng, CPU, bộ nhớ và PVC.
\item Bảng điều khiển \texttt{cutover} sau hoán đổi, thể hiện trạng thái \texttt{READY}, thời lượng và lỗi truy vấn.
\item Bảng điều khiển \texttt{data-integrity} sau chuyển giao; giá trị không khớp bằng \texttt{1} là kết quả dự kiến khi so hai cách tính khác nhau.
\end{enumerate}
